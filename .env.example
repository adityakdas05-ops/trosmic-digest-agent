# Copy to .env and fill only the values you need.
# Never commit real keys or secrets.

# Optional OpenAI/Band runtime values
OPENAI_API_KEY=
BAND_API_KEY=
BAND_AGENT_ID=

# Optional digest defaults
TROSMIC_CONFIG=agent_config.yaml
