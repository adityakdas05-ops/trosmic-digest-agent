# Config

Keep committed configuration templates and notes here. Local runtime files with secrets or machine-specific settings should stay out of Git.

Ignored local files:

- `../.env`
- `../agent_config.yaml`

Use the root-level examples as starting points:

- `../.env.example`
- `../agent_config.example.yaml`
